{
  "name": "sofinda",
  "private": true,
  "description": "Sofinda — property sub-ledger and operational intelligence platform (PRD v5.0)",
  "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@nestjs/platform-express": "^11.0.1"
  },
  "scripts": {
    "dev:api": "npm run start:dev -w api",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w api && npm run build -w web",
    "lint": "npm run lint -w api --if-present && npm run lint -w web --if-present"
  }
}
